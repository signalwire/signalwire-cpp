build/
build-tidy/
build-asan/
__pycache__/

# Claude Code local session settings (not part of the SDK)
.claude/

# sw-verify repo-local scratch
.sw-tmp/

# Foreign-toolchain (php) config accidentally committed into the C++ repo
.php-cs-fixer.cache
.php-cs-fixer.dist.php
.php-cs-fixer.php

# Test scratch fixtures (config-loader probe outputs) — not source
.sw-test-tmp/

# Track the shared Claude Code permission set (rest of .claude/ stays local)
!.claude/
.claude/*
!.claude/settings.json
