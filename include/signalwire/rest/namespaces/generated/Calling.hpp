// Copyright (c) 2025 SignalWire
// SPDX-License-Identifier: MIT
//
// Code generated by scripts/generate_rest.py; DO NOT EDIT.
//
// AUTO-GENERATED from porting-sdk/rest-apis/ (x-sdk-* markup) — regenerate with:
//   python3 scripts/generate_rest.py
//
// Generated command-dispatch resource for the 'calling' namespace.
#pragma once

#include <map>
#include <nlohmann/json.hpp>
#include <optional>
#include <string>

#include "signalwire/rest/base_resource.hpp"

namespace signalwire {
namespace rest {
namespace generated {

using json = nlohmann::json;

/// Calling — command-dispatch resource (calling spec).
/// Each method POSTs {command, params, id?} to /api/calling/calls.
class Calling {
 public:
  struct DialParams {
    std::string from;
    std::string to;
    std::optional<std::string> caller_id;
    std::optional<std::string> fallback_url;
    std::optional<std::string> status_url;
    std::optional<json> status_events;
    std::optional<std::string> url_method;
    std::optional<std::string> url;
    std::optional<json> codecs;
    std::optional<json> swml;
    json extras = json::object();
  };

  struct UpdateParams {
    std::string id;
    std::optional<std::string> fallback_url;
    std::optional<std::string> status;
    std::optional<std::string> status_url;
    std::optional<std::string> url;
    std::optional<json> swml;
    json extras = json::object();
  };

  struct EndParams {
    std::optional<std::string> reason;
    json extras = json::object();
  };

  struct Ai_holdParams {
    std::optional<int> timeout;
    std::optional<std::string> prompt;
    json extras = json::object();
  };

  struct Ai_unholdParams {
    std::optional<std::string> prompt;
    json extras = json::object();
  };

  struct Ai_messageParams {
    std::optional<std::string> role;
    std::optional<std::string> message_text;
    std::optional<json> reset;
    std::optional<json> global_data;
    json extras = json::object();
  };

  struct Live_transcribeParams {
    json action;
    json extras = json::object();
  };

  struct Live_translateParams {
    json action;
    std::optional<std::string> status_url;
    json extras = json::object();
  };

  struct TransferParams {
    json dest;
    json extras = json::object();
  };

  struct User_eventParams {
    json event;
    json extras = json::object();
  };

  struct DisconnectParams {
    json extras = json::object();
  };

  struct PlayParams {
    json play;
    std::optional<std::string> control_id;
    std::optional<double> volume;
    std::optional<std::string> direction;
    std::optional<int> loop;
    std::optional<std::string> status_url;
    json extras = json::object();
  };

  struct Play_pauseParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Play_resumeParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Play_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Play_volumeParams {
    std::string control_id;
    double volume;
    json extras = json::object();
  };

  struct RecordParams {
    std::optional<std::string> control_id;
    std::optional<json> audio;
    std::optional<std::string> status_url;
    json extras = json::object();
  };

  struct Record_pauseParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Record_resumeParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Record_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct CollectParams {
    std::optional<std::string> control_id;
    std::optional<double> initial_timeout;
    std::optional<json> digits;
    std::optional<json> speech;
    std::optional<bool> continuous;
    std::optional<bool> partial_results;
    json extras = json::object();
  };

  struct Collect_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Collect_start_input_timersParams {
    std::string control_id;
    json extras = json::object();
  };

  struct DetectParams {
    json detect;
    std::optional<std::string> control_id;
    std::optional<double> timeout;
    json extras = json::object();
  };

  struct Detect_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct TapParams {
    json tap;
    json device;
    std::optional<std::string> control_id;
    json extras = json::object();
  };

  struct Tap_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct StreamParams {
    std::string url;
    std::optional<std::string> control_id;
    std::optional<std::string> codec;
    std::optional<std::string> track;
    std::optional<std::string> authorization_bearer_token;
    std::optional<json> custom_parameters;
    json extras = json::object();
  };

  struct Stream_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct DenoiseParams {
    json extras = json::object();
  };

  struct Denoise_stopParams {
    json extras = json::object();
  };

  struct TranscribeParams {
    std::optional<std::string> control_id;
    std::optional<std::string> status_url;
    json extras = json::object();
  };

  struct Transcribe_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Ai_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Send_fax_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct Receive_fax_stopParams {
    std::string control_id;
    json extras = json::object();
  };

  struct ReferParams {
    json device;
    std::optional<std::string> status_url;
    json extras = json::object();
  };

  explicit Calling(const HttpClient& http) : http_(http) {}

  static constexpr const char* kBasePath = "/api/calling/calls";

  [[nodiscard]] json dial(const DialParams& p) const {
    json params = json::object();
    params["from"] = p.from;
    params["to"] = p.to;
    if (p.caller_id.has_value()) {
      params["caller_id"] = *p.caller_id;
    }
    if (p.fallback_url.has_value()) {
      params["fallback_url"] = *p.fallback_url;
    }
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (p.status_events.has_value()) {
      params["status_events"] = *p.status_events;
    }
    if (p.url_method.has_value()) {
      params["url_method"] = *p.url_method;
    }
    if (p.url.has_value()) {
      params["url"] = *p.url;
    }
    if (p.codecs.has_value()) {
      params["codecs"] = *p.codecs;
    }
    if (p.swml.has_value()) {
      params["swml"] = *p.swml;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("dial", params);
  }
  [[nodiscard]] json update(const UpdateParams& p) const {
    json params = json::object();
    params["id"] = p.id;
    if (p.fallback_url.has_value()) {
      params["fallback_url"] = *p.fallback_url;
    }
    if (p.status.has_value()) {
      params["status"] = *p.status;
    }
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (p.url.has_value()) {
      params["url"] = *p.url;
    }
    if (p.swml.has_value()) {
      params["swml"] = *p.swml;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("update", params);
  }
  [[nodiscard]] json end(const std::string& call_id, const EndParams& p) const {
    json params = json::object();
    if (p.reason.has_value()) {
      params["reason"] = *p.reason;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.end", params, call_id);
  }
  [[nodiscard]] json ai_hold(const std::string& call_id, const Ai_holdParams& p) const {
    json params = json::object();
    if (p.timeout.has_value()) {
      params["timeout"] = *p.timeout;
    }
    if (p.prompt.has_value()) {
      params["prompt"] = *p.prompt;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.ai_hold", params, call_id);
  }
  [[nodiscard]] json ai_unhold(const std::string& call_id, const Ai_unholdParams& p) const {
    json params = json::object();
    if (p.prompt.has_value()) {
      params["prompt"] = *p.prompt;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.ai_unhold", params, call_id);
  }
  [[nodiscard]] json ai_message(const std::string& call_id, const Ai_messageParams& p) const {
    json params = json::object();
    if (p.role.has_value()) {
      params["role"] = *p.role;
    }
    if (p.message_text.has_value()) {
      params["message_text"] = *p.message_text;
    }
    if (p.reset.has_value()) {
      params["reset"] = *p.reset;
    }
    if (p.global_data.has_value()) {
      params["global_data"] = *p.global_data;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.ai_message", params, call_id);
  }
  [[nodiscard]] json live_transcribe(const std::string& call_id,
                                     const Live_transcribeParams& p) const {
    json params = json::object();
    params["action"] = p.action;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.live_transcribe", params, call_id);
  }
  [[nodiscard]] json live_translate(const std::string& call_id,
                                    const Live_translateParams& p) const {
    json params = json::object();
    params["action"] = p.action;
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.live_translate", params, call_id);
  }
  [[nodiscard]] json transfer(const std::string& call_id, const TransferParams& p) const {
    json params = json::object();
    params["dest"] = p.dest;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.transfer", params, call_id);
  }
  [[nodiscard]] json user_event(const std::string& call_id, const User_eventParams& p) const {
    json params = json::object();
    params["event"] = p.event;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.user_event", params, call_id);
  }
  [[nodiscard]] json disconnect(const std::string& call_id, const DisconnectParams& p) const {
    json params = json::object();
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.disconnect", params, call_id);
  }
  [[nodiscard]] json play(const std::string& call_id, const PlayParams& p) const {
    json params = json::object();
    params["play"] = p.play;
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.volume.has_value()) {
      params["volume"] = *p.volume;
    }
    if (p.direction.has_value()) {
      params["direction"] = *p.direction;
    }
    if (p.loop.has_value()) {
      params["loop"] = *p.loop;
    }
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.play", params, call_id);
  }
  [[nodiscard]] json play_pause(const std::string& call_id, const Play_pauseParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.play.pause", params, call_id);
  }
  [[nodiscard]] json play_resume(const std::string& call_id, const Play_resumeParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.play.resume", params, call_id);
  }
  [[nodiscard]] json play_stop(const std::string& call_id, const Play_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.play.stop", params, call_id);
  }
  [[nodiscard]] json play_volume(const std::string& call_id, const Play_volumeParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    params["volume"] = p.volume;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.play.volume", params, call_id);
  }
  [[nodiscard]] json record(const std::string& call_id, const RecordParams& p) const {
    json params = json::object();
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.audio.has_value()) {
      params["audio"] = *p.audio;
    }
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.record", params, call_id);
  }
  [[nodiscard]] json record_pause(const std::string& call_id, const Record_pauseParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.record.pause", params, call_id);
  }
  [[nodiscard]] json record_resume(const std::string& call_id, const Record_resumeParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.record.resume", params, call_id);
  }
  [[nodiscard]] json record_stop(const std::string& call_id, const Record_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.record.stop", params, call_id);
  }
  [[nodiscard]] json collect(const std::string& call_id, const CollectParams& p) const {
    json params = json::object();
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.initial_timeout.has_value()) {
      params["initial_timeout"] = *p.initial_timeout;
    }
    if (p.digits.has_value()) {
      params["digits"] = *p.digits;
    }
    if (p.speech.has_value()) {
      params["speech"] = *p.speech;
    }
    if (p.continuous.has_value()) {
      params["continuous"] = *p.continuous;
    }
    if (p.partial_results.has_value()) {
      params["partial_results"] = *p.partial_results;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.collect", params, call_id);
  }
  [[nodiscard]] json collect_stop(const std::string& call_id, const Collect_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.collect.stop", params, call_id);
  }
  [[nodiscard]] json collect_start_input_timers(const std::string& call_id,
                                                const Collect_start_input_timersParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.collect.start_input_timers", params, call_id);
  }
  [[nodiscard]] json detect(const std::string& call_id, const DetectParams& p) const {
    json params = json::object();
    params["detect"] = p.detect;
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.timeout.has_value()) {
      params["timeout"] = *p.timeout;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.detect", params, call_id);
  }
  [[nodiscard]] json detect_stop(const std::string& call_id, const Detect_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.detect.stop", params, call_id);
  }
  [[nodiscard]] json tap(const std::string& call_id, const TapParams& p) const {
    json params = json::object();
    params["tap"] = p.tap;
    params["device"] = p.device;
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.tap", params, call_id);
  }
  [[nodiscard]] json tap_stop(const std::string& call_id, const Tap_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.tap.stop", params, call_id);
  }
  [[nodiscard]] json stream(const std::string& call_id, const StreamParams& p) const {
    json params = json::object();
    params["url"] = p.url;
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.codec.has_value()) {
      params["codec"] = *p.codec;
    }
    if (p.track.has_value()) {
      params["track"] = *p.track;
    }
    if (p.authorization_bearer_token.has_value()) {
      params["authorization_bearer_token"] = *p.authorization_bearer_token;
    }
    if (p.custom_parameters.has_value()) {
      params["custom_parameters"] = *p.custom_parameters;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.stream", params, call_id);
  }
  [[nodiscard]] json stream_stop(const std::string& call_id, const Stream_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.stream.stop", params, call_id);
  }
  [[nodiscard]] json denoise(const std::string& call_id, const DenoiseParams& p) const {
    json params = json::object();
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.denoise", params, call_id);
  }
  [[nodiscard]] json denoise_stop(const std::string& call_id, const Denoise_stopParams& p) const {
    json params = json::object();
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.denoise.stop", params, call_id);
  }
  [[nodiscard]] json transcribe(const std::string& call_id, const TranscribeParams& p) const {
    json params = json::object();
    if (p.control_id.has_value()) {
      params["control_id"] = *p.control_id;
    }
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.transcribe", params, call_id);
  }
  [[nodiscard]] json transcribe_stop(const std::string& call_id,
                                     const Transcribe_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.transcribe.stop", params, call_id);
  }
  [[nodiscard]] json ai_stop(const std::string& call_id, const Ai_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.ai.stop", params, call_id);
  }
  [[nodiscard]] json send_fax_stop(const std::string& call_id, const Send_fax_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.send_fax.stop", params, call_id);
  }
  [[nodiscard]] json receive_fax_stop(const std::string& call_id,
                                      const Receive_fax_stopParams& p) const {
    json params = json::object();
    params["control_id"] = p.control_id;
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.receive_fax.stop", params, call_id);
  }
  [[nodiscard]] json refer(const std::string& call_id, const ReferParams& p) const {
    json params = json::object();
    params["device"] = p.device;
    if (p.status_url.has_value()) {
      params["status_url"] = *p.status_url;
    }
    if (!p.extras.is_null()) {
      params.update(p.extras);
    }
    return execute("calling.refer", params, call_id);
  }

 private:
  [[nodiscard]] json execute(const std::string& command, const json& params,
                             const std::optional<std::string>& call_id = std::nullopt) const {
    json body = {{"command", command}, {"params", params}};
    if (call_id.has_value()) {
      body["id"] = *call_id;
    }
    return http_.post(kBasePath, body);
  }

  const HttpClient& http_;
};

}  // namespace generated
}  // namespace rest
}  // namespace signalwire
