// Auto-generated by gen_cov.py — DO NOT EDIT BY HAND.
// Full success+error REST coverage. One TEST per coverable canonical
// route: a 2xx call and a staged-4xx call, each asserting the journaled
// matched_route + response_status. Included by tests/test_main.cpp.
#include "mocktest.hpp"

namespace {
using namespace signalwire::rest;
using nlohmann::json;
}  // namespace

TEST(rest_mock_cov_calling_call_commands) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("calling.call-commands", 200, json::object());
  (void)client.calling().dial({{"url", "https://x/swml"}, {"to", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("calling.call-commands"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("calling.call-commands", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.calling().dial({{"url", "https://x/swml"}, {"to", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("calling.call-commands"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_chat_create_chat_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("chat.create_chat_token", 200, json::object());
  (void)client.chat().create_token({{"ttl", 60}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("chat.create_chat_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("chat.create_chat_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.chat().create_token({{"ttl", 60}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("chat.create_chat_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_a_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_a_call", 200, json::object());
  (void)client.compat().calls.create({{"To", "+1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_a_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.create({{"To", "+1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_application", 200, json::object());
  (void)client.compat().applications.create({{"FriendlyName", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().applications.create({{"FriendlyName", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_conference_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_conference_stream", 200, json::object());
  (void)client.compat().conferences.start_stream("co-1", {{"Url", "wss://x"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_conference_stream", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.start_stream("co-1", {{"Url", "wss://x"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_cxml_script", 200, json::object());
  (void)client.compat().laml_bins.create({{"Name", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().laml_bins.create({{"Name", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_imported_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_imported_phone_number", 200, json::object());
  (void)client.compat().phone_numbers.import_number({{"PhoneNumber", "+1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_imported_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_imported_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.import_number({{"PhoneNumber", "+1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_imported_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_incoming_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_incoming_phone_number", 200, json::object());
  (void)client.compat().phone_numbers.purchase({{"PhoneNumber", "+1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_incoming_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.purchase({{"PhoneNumber", "+1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_message) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_message", 200, json::object());
  (void)client.compat().messages.create({{"To", "+1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_message", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.create({{"To", "+1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_queue", 200, json::object());
  (void)client.compat().queues.create({{"FriendlyName", "q"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.create({{"FriendlyName", "q"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_recording", 200, json::object());
  (void)client.compat().calls.start_recording("ca-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.start_recording("ca-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_stream", 200, json::object());
  (void)client.compat().calls.start_stream("ca-1", {{"Url", "wss://x"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.start_stream("ca-1", {{"Url", "wss://x"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_subprojects) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_subprojects", 200, json::object());
  (void)client.compat().accounts.create({{"FriendlyName", "n"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_subprojects"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_subprojects", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().accounts.create({{"FriendlyName", "n"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_subprojects"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_create_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.create_token", 200, json::object());
  (void)client.compat().tokens.create({{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.create_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().tokens.create({{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_a_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_a_call", 200, json::object());
  (void)client.compat().calls.del("ca-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_a_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.del("ca-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_application", 200, json::object());
  (void)client.compat().applications.del("ap-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().applications.del("ap-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_conference_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_conference_recording", 200, json::object());
  (void)client.compat().conferences.delete_recording("co-1", "re-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_conference_recording", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.delete_recording("co-1", "re-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_cxml_script", 200, json::object());
  (void)client.compat().laml_bins.del("lb-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().laml_bins.del("lb-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_fax) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_fax", 200, json::object());
  (void)client.compat().faxes.del("fa-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_fax", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.del("fa-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_fax_media) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_fax_media", 200, json::object());
  (void)client.compat().faxes.delete_media("fa-1", "me-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_fax_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_fax_media", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.delete_media("fa-1", "me-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_fax_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_incoming_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_incoming_phone_number", 200, json::object());
  (void)client.compat().phone_numbers.delete_("pn-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_incoming_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.delete_("pn-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_message) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_message", 200, json::object());
  (void)client.compat().messages.del("ms-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_message", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.del("ms-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_message_media) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_message_media", 200, json::object());
  (void)client.compat().messages.delete_media("ms-1", "me-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_message_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_message_media", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.delete_media("ms-1", "me-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_message_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_participant) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_participant", 200, json::object());
  (void)client.compat().conferences.remove_participant("co-1", "ca-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_participant", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.remove_participant("co-1", "ca-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_queue", 200, json::object());
  (void)client.compat().queues.del("qu-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.del("qu-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_recording", 200, json::object());
  (void)client.compat().recordings.delete_("re-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().recordings.delete_("re-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_token", 200, json::object());
  (void)client.compat().tokens.delete_("tk-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().tokens.delete_("tk-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_delete_transcription) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.delete_transcription", 200, json::object());
  (void)client.compat().transcriptions.delete_("tr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_transcription"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.delete_transcription", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().transcriptions.delete_("tr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.delete_transcription"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_get_account) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.get_account", 200, json::object());
  (void)client.compat().accounts.get("acc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_account"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.get_account", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().accounts.get("acc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_account"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_get_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.get_application", 200, json::object());
  (void)client.compat().applications.get("ap-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.get_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().applications.get("ap-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_get_conference_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.get_conference_recording", 200, json::object());
  (void)client.compat().conferences.get_recording("co-1", "re-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.get_conference_recording", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.get_recording("co-1", "re-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.get_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_accounts) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_accounts", 200, json::object());
  (void)client.compat().accounts.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_accounts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_accounts", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().accounts.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_accounts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_calls) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_calls", 200, json::object());
  (void)client.compat().calls.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_calls"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_calls", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_calls"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_conferences) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_conferences", 200, json::object());
  (void)client.compat().conferences.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_conferences", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_fax_media) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_fax_media", 200, json::object());
  (void)client.compat().faxes.list_media("fa-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_fax_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_fax_media", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.list_media("fa-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_fax_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_faxes) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_faxes", 200, json::object());
  (void)client.compat().faxes.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_faxes"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_faxes", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_faxes"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_participants) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_participants", 200, json::object());
  (void)client.compat().conferences.list_participants("co-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_participants"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_participants", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.list_participants("co-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_participants"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_all_queue_members) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_all_queue_members", 200, json::object());
  (void)client.compat().queues.list_members("qu-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_queue_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_all_queue_members", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.list_members("qu-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_all_queue_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_applications) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_applications", 200, json::object());
  (void)client.compat().applications.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_applications", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().applications.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_available_phone_number_resources) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_available_phone_number_resources", 200,
                         json::object());
  (void)client.compat().phone_numbers.list_available_countries();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_available_phone_number_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_available_phone_number_resources", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.list_available_countries();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_available_phone_number_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_conference_recordings) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_conference_recordings", 200, json::object());
  (void)client.compat().conferences.list_recordings("co-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_conference_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_conference_recordings", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.list_recordings("co-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_conference_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_cxml_scripts) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_cxml_scripts", 200, json::object());
  (void)client.compat().laml_bins.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_cxml_scripts", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().laml_bins.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_incoming_phone_numbers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_incoming_phone_numbers", 200, json::object());
  (void)client.compat().phone_numbers.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_incoming_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_incoming_phone_numbers", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_incoming_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_media) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_media", 200, json::object());
  (void)client.compat().messages.list_media("ms-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_media", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.list_media("ms-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_messages) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_messages", 200, json::object());
  (void)client.compat().messages.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_messages"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_messages", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_messages"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_queues) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_queues", 200, json::object());
  (void)client.compat().queues.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_queues"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_queues", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_queues"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_recordings) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_recordings", 200, json::object());
  (void)client.compat().recordings.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_recordings", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().recordings.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_list_transcriptions) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.list_transcriptions", 200, json::object());
  (void)client.compat().transcriptions.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_transcriptions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.list_transcriptions", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().transcriptions.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.list_transcriptions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_a_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_a_call", 200, json::object());
  (void)client.compat().calls.get("ca-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_a_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.get("ca-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_conference", 200, json::object());
  (void)client.compat().conferences.get("co-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.get("co-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_cxml_script", 200, json::object());
  (void)client.compat().laml_bins.get("lb-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().laml_bins.get("lb-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_fax) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_fax", 200, json::object());
  (void)client.compat().faxes.get("fa-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_fax", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.get("fa-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_incoming_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_incoming_phone_number", 200, json::object());
  (void)client.compat().phone_numbers.get("pn-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_incoming_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.get("pn-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_media) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_media", 200, json::object());
  (void)client.compat().messages.get_media("ms-1", "me-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_media", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.get_media("ms-1", "me-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_media"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_medias) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_medias", 200, json::object());
  (void)client.compat().faxes.get_media("fa-1", "me-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_medias"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_medias", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.get_media("fa-1", "me-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_medias"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_message) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_message", 200, json::object());
  (void)client.compat().messages.get("ms-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_message", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.get("ms-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_participant) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_participant", 200, json::object());
  (void)client.compat().conferences.get_participant("co-1", "ca-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_participant", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.get_participant("co-1", "ca-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_queue", 200, json::object());
  (void)client.compat().queues.get("qu-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.get("qu-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_queue_member) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_queue_member", 200, json::object());
  (void)client.compat().queues.get_member("qu-1", "ca-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_queue_member", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.get_member("qu-1", "ca-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_recording", 200, json::object());
  (void)client.compat().recordings.get("re-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().recordings.get("re-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_retrieve_transcription) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.retrieve_transcription", 200, json::object());
  (void)client.compat().transcriptions.get("tr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_transcription"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.retrieve_transcription", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().transcriptions.get("tr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.retrieve_transcription"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_search_local_available_phone_numbers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.search_local_available_phone_numbers", 200, json::object());
  (void)client.compat().phone_numbers.search_local("US");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.search_local_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.search_local_available_phone_numbers", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.search_local("US");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.search_local_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_search_toll_free_available_phone_numbers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.search_toll_free_available_phone_numbers", 200,
                         json::object());
  (void)client.compat().phone_numbers.search_toll_free("US");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route,
              std::string("compatibility.search_toll_free_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.search_toll_free_available_phone_numbers", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.search_toll_free("US");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route,
              std::string("compatibility.search_toll_free_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_send_fax) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.send_fax", 200, json::object());
  (void)client.compat().faxes.create({{"To", "+1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.send_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.send_fax", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.create({{"To", "+1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.send_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_a_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_a_call", 200, json::object());
  (void)client.compat().calls.update("ca-1", {{"Status", "completed"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_a_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.update("ca-1", {{"Status", "completed"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_a_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_account) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_account", 200, json::object());
  (void)client.compat().accounts.update("acc-1", {{"FriendlyName", "n2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_account"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_account", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().accounts.update("acc-1", {{"FriendlyName", "n2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_account"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_application", 200, json::object());
  (void)client.compat().applications.update("ap-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().applications.update("ap-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_conference", 200, json::object());
  (void)client.compat().conferences.update("co-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.update("co-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_conference_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_conference_recording", 200, json::object());
  (void)client.compat().conferences.update_recording("co-1", "re-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_conference_recording", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.update_recording("co-1", "re-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_conference_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_conference_stream", 200, json::object());
  (void)client.compat().conferences.stop_stream("co-1", "str-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_conference_stream", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.stop_stream("co-1", "str-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_cxml_script", 200, json::object());
  (void)client.compat().laml_bins.update("lb-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().laml_bins.update("lb-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_fax) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_fax", 200, json::object());
  (void)client.compat().faxes.update("fa-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_fax", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().faxes.update("fa-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_fax"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_incoming_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_incoming_phone_number", 200, json::object());
  (void)client.compat().phone_numbers.update("pn-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_incoming_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().phone_numbers.update("pn-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_incoming_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_message) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_message", 200, json::object());
  (void)client.compat().messages.update("ms-1", {{"Body", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_message", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().messages.update("ms-1", {{"Body", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_message"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_participant) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_participant", 200, json::object());
  (void)client.compat().conferences.update_participant("co-1", "ca-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_participant", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().conferences.update_participant("co-1", "ca-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_participant"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_queue", 200, json::object());
  (void)client.compat().queues.update("qu-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.update("qu-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_queue_member) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_queue_member", 200, json::object());
  (void)client.compat().queues.dequeue_member("qu-1", "ca-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_queue_member", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().queues.dequeue_member("qu-1", "ca-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_recording", 200, json::object());
  (void)client.compat().calls.update_recording("ca-1", "re-1", {{"Status", "stopped"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.update_recording("ca-1", "re-1", {{"Status", "stopped"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_stream", 200, json::object());
  (void)client.compat().calls.stop_stream("ca-1", "str-1", {{"Status", "stopped"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().calls.stop_stream("ca-1", "str-1", {{"Status", "stopped"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_compatibility_update_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("compatibility.update_token", 200, json::object());
  (void)client.compat().tokens.update("tk-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("compatibility.update_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.compat().tokens.update("tk-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("compatibility.update_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_create_document) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.create_document", 200, json::object());
  (void)client.datasphere().documents.create({{"name", "d"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.create_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.create_document", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.create({{"name", "d"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.create_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_delete_document) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.delete_document", 200, json::object());
  (void)client.datasphere().documents.del("doc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.delete_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.delete_document", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.del("doc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.delete_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_delete_document_chunk) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.delete_document_chunk", 200, json::object());
  (void)client.datasphere().documents.delete_chunk("doc-1", "ch-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.delete_document_chunk"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.delete_document_chunk", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.delete_chunk("doc-1", "ch-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.delete_document_chunk"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_get_document) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.get_document", 200, json::object());
  (void)client.datasphere().documents.get("doc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.get_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.get_document", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.get("doc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.get_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_get_document_chunk) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.get_document_chunk", 200, json::object());
  (void)client.datasphere().documents.get_chunk("doc-1", "ch-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.get_document_chunk"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.get_document_chunk", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.get_chunk("doc-1", "ch-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.get_document_chunk"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_list_document_chunks) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.list_document_chunks", 200, json::object());
  (void)client.datasphere().documents.list_chunks("doc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.list_document_chunks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.list_document_chunks", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.list_chunks("doc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.list_document_chunks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_list_documents) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.list_documents", 200, json::object());
  (void)client.datasphere().documents.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.list_documents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.list_documents", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.list_documents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_search_documents) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.search_documents", 200, json::object());
  (void)client.datasphere().documents.search({{"query", "q"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.search_documents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.search_documents", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.search({{"query", "q"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.search_documents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_datasphere_update_document) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("datasphere.update_document", 200, json::object());
  (void)client.datasphere().documents.update("doc-1", {{"name", "d2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.update_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("datasphere.update_document", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.datasphere().documents.update("doc-1", {{"name", "d2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("datasphere.update_document"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_assign_resource_domain_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_domain_application", 200, json::object());
  (void)client.fabric().resources.assign_domain_application("res-1",
                                                            {{"domain_application_id", "da-1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_domain_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.assign_resource_domain_application", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.assign_domain_application("res-1",
                                                              {{"domain_application_id", "da-1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_domain_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_assign_resource_phone_route) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_phone_route", 200, json::object());
  (void)client.fabric().resources.assign_phone_route("res-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_phone_route"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.assign_resource_phone_route", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.assign_phone_route("res-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_phone_route"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_ai_agent) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_ai_agent", 200, json::object());
  (void)client.fabric().ai_agents.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_ai_agent", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_call_flow) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_call_flow", 200, json::object());
  (void)client.fabric().call_flows.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_call_flow", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_conference_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_conference_room", 200, json::object());
  (void)client.fabric().conference_rooms.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_conference_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_script", 200, json::object());
  (void)client.fabric().cxml_scripts.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_cxml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_webhook", 200, json::object());
  (void)client.fabric().cxml_webhooks.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_cxml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_embeds_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_embeds_token", 200, json::object());
  (void)client.fabric().tokens.create_embed_token({{"a", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_embeds_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_embeds_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().tokens.create_embed_token({{"a", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_embeds_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_freeswitch_connector) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_freeswitch_connector", 200, json::object());
  (void)client.fabric().freeswitch_connectors.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_freeswitch_connector", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_relay_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_relay_application", 200, json::object());
  (void)client.fabric().relay_applications.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_relay_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_endpoint", 200, json::object());
  (void)client.fabric().sip_endpoints.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_sip_endpoint", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_sip_gateway) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_gateway", 200, json::object());
  (void)client.fabric().sip_gateways.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_sip_gateway", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_gateways.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_subscriber) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber", 200, json::object());
  (void)client.fabric().subscribers.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_subscriber", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_subscriber_guest_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_guest_token", 200, json::object());
  (void)client.fabric().tokens.create_guest_token({{"a", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_guest_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_subscriber_guest_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().tokens.create_guest_token({{"a", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_guest_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_subscriber_invite_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_invite_token", 200, json::object());
  (void)client.fabric().tokens.create_invite_token({{"email", "e@x.com"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_invite_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_subscriber_invite_token", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().tokens.create_invite_token({{"email", "e@x.com"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_invite_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_subscriber_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_sip_endpoint", 200, json::object());
  (void)client.fabric().subscribers.create_sip_endpoint("id-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_subscriber_sip_endpoint", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.create_sip_endpoint("id-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_subscriber_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_token", 200, json::object());
  (void)client.fabric().tokens.create_subscriber_token({{"a", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_subscriber_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().tokens.create_subscriber_token({{"a", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_swml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_script", 200, json::object());
  (void)client.fabric().swml_scripts.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_swml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_create_swml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_webhook", 200, json::object());
  (void)client.fabric().swml_webhooks.create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.create_swml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_ai_agent) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_ai_agent", 200, json::object());
  (void)client.fabric().ai_agents.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_ai_agent", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_call_flow) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_call_flow", 200, json::object());
  (void)client.fabric().call_flows.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_call_flow", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_conference_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_conference_room", 200, json::object());
  (void)client.fabric().conference_rooms.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_conference_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_cxml_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_application", 200, json::object());
  (void)client.fabric().cxml_applications.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_cxml_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_applications.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_script", 200, json::object());
  (void)client.fabric().cxml_scripts.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_cxml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_webhook", 200, json::object());
  (void)client.fabric().cxml_webhooks.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_cxml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_freeswitch_connector) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_freeswitch_connector", 200, json::object());
  (void)client.fabric().freeswitch_connectors.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_freeswitch_connector", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_relay_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_relay_application", 200, json::object());
  (void)client.fabric().relay_applications.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_relay_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_resource) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_resource", 200, json::object());
  (void)client.fabric().resources.delete_("res-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_resource", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.delete_("res-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_endpoint", 200, json::object());
  (void)client.fabric().sip_endpoints.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_sip_endpoint", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_sip_gateway) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_gateway", 200, json::object());
  (void)client.fabric().sip_gateways.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_sip_gateway", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_gateways.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_subscriber) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber", 200, json::object());
  (void)client.fabric().subscribers.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_subscriber", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_subscriber_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber_sip_endpoint", 200, json::object());
  (void)client.fabric().subscribers.delete_sip_endpoint("id-1", "ep-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_subscriber_sip_endpoint", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.delete_sip_endpoint("id-1", "ep-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_swml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_script", 200, json::object());
  (void)client.fabric().swml_scripts.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_swml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_delete_swml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_webhook", 200, json::object());
  (void)client.fabric().swml_webhooks.del("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.delete_swml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.del("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_deploy_call_flow_version) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.deploy_call_flow_version", 200, json::object());
  (void)client.fabric().call_flows.deploy_version("id-1", {{"v", "1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.deploy_call_flow_version"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.deploy_call_flow_version", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.deploy_version("id-1", {{"v", "1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.deploy_call_flow_version"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_ai_agent) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_ai_agent", 200, json::object());
  (void)client.fabric().ai_agents.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_ai_agent", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_call_flow) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_call_flow", 200, json::object());
  (void)client.fabric().call_flows.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_call_flow", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_conference_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_conference_room", 200, json::object());
  (void)client.fabric().conference_rooms.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_conference_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_cxml_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_application", 200, json::object());
  (void)client.fabric().cxml_applications.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_cxml_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_applications.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_script", 200, json::object());
  (void)client.fabric().cxml_scripts.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_cxml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_webhook", 200, json::object());
  (void)client.fabric().cxml_webhooks.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_cxml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_fabric_address) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_fabric_address", 200, json::object());
  (void)client.fabric().addresses.get("addr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_fabric_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_fabric_address", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().addresses.get("addr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_fabric_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_freeswitch_connector) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_freeswitch_connector", 200, json::object());
  (void)client.fabric().freeswitch_connectors.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_freeswitch_connector", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_relay_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_relay_application", 200, json::object());
  (void)client.fabric().relay_applications.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_relay_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_resource) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_resource", 200, json::object());
  (void)client.fabric().resources.get("res-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_resource", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.get("res-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_endpoint", 200, json::object());
  (void)client.fabric().sip_endpoints.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_sip_endpoint", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_sip_gateway) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_gateway", 200, json::object());
  (void)client.fabric().sip_gateways.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_sip_gateway", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_gateways.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_subscriber) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber", 200, json::object());
  (void)client.fabric().subscribers.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_subscriber", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_subscriber_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber_sip_endpoint", 200, json::object());
  (void)client.fabric().subscribers.get_sip_endpoint("id-1", "ep-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_subscriber_sip_endpoint", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.get_sip_endpoint("id-1", "ep-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_swml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_script", 200, json::object());
  (void)client.fabric().swml_scripts.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_swml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_get_swml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_webhook", 200, json::object());
  (void)client.fabric().swml_webhooks.get("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.get_swml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.get("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_ai_agent_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agent_addresses", 200, json::object());
  (void)client.fabric().ai_agents.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agent_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_ai_agent_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agent_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_ai_agents) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agents", 200, json::object());
  (void)client.fabric().ai_agents.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_ai_agents", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_call_flow_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_addresses", 200, json::object());
  (void)client.fabric().call_flows.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_call_flow_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_call_flow_versions) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_versions", 200, json::object());
  (void)client.fabric().call_flows.list_versions("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_versions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_call_flow_versions", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.list_versions("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_versions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_call_flows) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flows", 200, json::object());
  (void)client.fabric().call_flows.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flows"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_call_flows", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flows"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_conference_room_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_room_addresses", 200, json::object());
  (void)client.fabric().conference_rooms.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_room_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_conference_room_addresses", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_room_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_conference_rooms) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_rooms", 200, json::object());
  (void)client.fabric().conference_rooms.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_conference_rooms", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_application_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_application_addresses", 200, json::object());
  (void)client.fabric().cxml_applications.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_application_addresses", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_applications.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_applications) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_applications", 200, json::object());
  (void)client.fabric().cxml_applications.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_applications", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_applications.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_script_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_script_addresses", 200, json::object());
  (void)client.fabric().cxml_scripts.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_script_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_scripts) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_scripts", 200, json::object());
  (void)client.fabric().cxml_scripts.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_scripts", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_webhook_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhook_addresses", 200, json::object());
  (void)client.fabric().cxml_webhooks.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_webhook_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_cxml_webhooks) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhooks", 200, json::object());
  (void)client.fabric().cxml_webhooks.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_cxml_webhooks", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_fabric_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_fabric_addresses", 200, json::object());
  (void)client.fabric().addresses.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_fabric_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_fabric_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().addresses.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_fabric_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_freeswitch_connector_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connector_addresses", 200, json::object());
  (void)client.fabric().freeswitch_connectors.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connector_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_freeswitch_connector_addresses", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connector_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_freeswitch_connectors) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connectors", 200, json::object());
  (void)client.fabric().freeswitch_connectors.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connectors"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_freeswitch_connectors", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connectors"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_relay_application_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_application_addresses", 200, json::object());
  (void)client.fabric().relay_applications.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_relay_application_addresses", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_relay_applications) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_applications", 200, json::object());
  (void)client.fabric().relay_applications.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_relay_applications", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_resource_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resource_addresses", 200, json::object());
  (void)client.fabric().resources.list_addresses("res-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resource_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_resource_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.list_addresses("res-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resource_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_resources) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resources", 200, json::object());
  (void)client.fabric().resources.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_resources", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().resources.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_sip_endpoint_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoint_addresses", 200, json::object());
  (void)client.fabric().sip_endpoints.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoint_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_sip_endpoint_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoint_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_sip_endpoints) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoints", 200, json::object());
  (void)client.fabric().sip_endpoints.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_sip_endpoints", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_sip_gateways) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_gateways", 200, json::object());
  (void)client.fabric().sip_gateways.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateways"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_sip_gateways", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_gateways.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateways"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_subscriber_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_addresses", 200, json::object());
  (void)client.fabric().subscribers.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_subscriber_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_subscriber_sip_endpoints) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_sip_endpoints", 200, json::object());
  (void)client.fabric().subscribers.list_sip_endpoints("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_subscriber_sip_endpoints", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.list_sip_endpoints("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_subscribers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscribers", 200, json::object());
  (void)client.fabric().subscribers.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscribers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_subscribers", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscribers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_swml_script_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_script_addresses", 200, json::object());
  (void)client.fabric().swml_scripts.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_swml_script_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_swml_scripts) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_scripts", 200, json::object());
  (void)client.fabric().swml_scripts.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_swml_scripts", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_swml_webhook_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhook_addresses", 200, json::object());
  (void)client.fabric().swml_webhooks.list_addresses("id-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_swml_webhook_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.list_addresses("id-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_list_swml_webhooks) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhooks", 200, json::object());
  (void)client.fabric().swml_webhooks.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.list_swml_webhooks", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_refresh_subscriber_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.refresh_subscriber_token", 200, json::object());
  (void)client.fabric().tokens.refresh_subscriber_token({{"refresh_token", "r"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.refresh_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.refresh_subscriber_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().tokens.refresh_subscriber_token({{"refresh_token", "r"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.refresh_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_ai_agent) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_ai_agent", 200, json::object());
  (void)client.fabric().ai_agents.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_ai_agent", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().ai_agents.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_call_flow) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_call_flow", 200, json::object());
  (void)client.fabric().call_flows.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_call_flow", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().call_flows.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_conference_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_conference_room", 200, json::object());
  (void)client.fabric().conference_rooms.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_conference_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().conference_rooms.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_cxml_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_application", 200, json::object());
  (void)client.fabric().cxml_applications.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_cxml_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_applications.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_cxml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_script", 200, json::object());
  (void)client.fabric().cxml_scripts.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_cxml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_scripts.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_cxml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_webhook", 200, json::object());
  (void)client.fabric().cxml_webhooks.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_cxml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().cxml_webhooks.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_freeswitch_connector) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_freeswitch_connector", 200, json::object());
  (void)client.fabric().freeswitch_connectors.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_freeswitch_connector", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().freeswitch_connectors.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_relay_application) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_relay_application", 200, json::object());
  (void)client.fabric().relay_applications.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_relay_application", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().relay_applications.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_endpoint", 200, json::object());
  (void)client.fabric().sip_endpoints.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_sip_endpoint", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_endpoints.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_sip_gateway) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_gateway", 200, json::object());
  (void)client.fabric().sip_gateways.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_sip_gateway", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().sip_gateways.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_subscriber) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber", 200, json::object());
  (void)client.fabric().subscribers.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_subscriber", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_subscriber_sip_endpoint) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber_sip_endpoint", 200, json::object());
  (void)client.fabric().subscribers.update_sip_endpoint("id-1", "ep-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_subscriber_sip_endpoint", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().subscribers.update_sip_endpoint("id-1", "ep-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_swml_script) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_script", 200, json::object());
  (void)client.fabric().swml_scripts.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_swml_script", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_scripts.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fabric_update_swml_webhook) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_webhook", 200, json::object());
  (void)client.fabric().swml_webhooks.update("id-1", {{"name", "a2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fabric.update_swml_webhook", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.fabric().swml_webhooks.update("id-1", {{"name", "a2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fax_get_fax_log) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fax.get_fax_log", 200, json::object());
  (void)client.logs().fax.get("fl-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fax.get_fax_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fax.get_fax_log", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().fax.get("fl-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fax.get_fax_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_fax_list_fax_logs) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fax.list_fax_logs", 200, json::object());
  (void)client.logs().fax.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fax.list_fax_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("fax.list_fax_logs", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().fax.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fax.list_fax_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_logs_list_conferences) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("logs.list_conferences", 200, json::object());
  (void)client.logs().conferences.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("logs.list_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("logs.list_conferences", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().conferences.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("logs.list_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_message_get_message_log) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("message.get_message_log", 200, json::object());
  (void)client.logs().messages.get("ml-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("message.get_message_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("message.get_message_log", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().messages.get("ml-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("message.get_message_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_message_list_message_logs) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("message.list_message_logs", 200, json::object());
  (void)client.logs().messages.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("message.list_message_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("message.list_message_logs", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().messages.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("message.list_message_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_project_create_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("project.create_token", 200, json::object());
  (void)client.project().tokens.create({{"name", "t"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("project.create_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.project().tokens.create({{"name", "t"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_project_delete_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("project.delete_token", 200, json::object());
  (void)client.project().tokens.delete_("tok-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.delete_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("project.delete_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.project().tokens.delete_("tok-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.delete_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_project_update_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("project.update_token", 200, json::object());
  (void)client.project().tokens.update("tok-1", {{"name", "t2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.update_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("project.update_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.project().tokens.update("tok-1", {{"name", "t2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("project.update_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_pubsub_create_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("pubsub.create_token", 200, json::object());
  (void)client.pubsub().create_token({{"ttl", 60}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("pubsub.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("pubsub.create_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.pubsub().create_token({{"ttl", 60}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("pubsub.create_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_address) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_address", 200, json::object());
  (void)client.addresses().create({{"name", "a"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_address", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.addresses().create({{"name", "a"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_brand) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_brand", 200, json::object());
  (void)client.registry().brands.create({{"name", "b"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_brand"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_brand", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().brands.create({{"name", "b"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_brand"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_campaign) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_campaign", 200, json::object());
  (void)client.registry().brands.create_campaign("br-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_campaign", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().brands.create_campaign("br-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_imported_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_imported_phone_number", 200, json::object());
  (void)client.imported_numbers().create({{"number", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_imported_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_imported_phone_number", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.imported_numbers().create({{"number", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_imported_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_number_group) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_number_group", 200, json::object());
  (void)client.number_groups().create({{"name", "ng"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_number_group", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().create({{"name", "ng"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_number_group_membership) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_number_group_membership", 200, json::object());
  (void)client.number_groups().add_membership("ng-1", {{"number_id", "n-1"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_number_group_membership", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().add_membership("ng-1", {{"number_id", "n-1"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_order) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_order", 200, json::object());
  (void)client.registry().campaigns.create_order("cm-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_order"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_order", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().campaigns.create_order("cm-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_order"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_queue", 200, json::object());
  (void)client.queues().create({{"name", "q"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().create({{"name", "q"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_create_verified_caller_id) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.create_verified_caller_id", 200, json::object());
  (void)client.verified_callers().create({{"number", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.create_verified_caller_id", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().create({{"number", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.create_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_address) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_address", 200, json::object());
  (void)client.addresses().delete_("ad-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_address", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.addresses().delete_("ad-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_number_assignment) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_number_assignment", 200, json::object());
  (void)client.registry().numbers.delete_("nm-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_assignment"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_number_assignment", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().numbers.delete_("nm-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_assignment"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_number_group) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_number_group", 200, json::object());
  (void)client.number_groups().del("ng-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_number_group", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().del("ng-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_number_group_membership) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_number_group_membership", 200, json::object());
  (void)client.number_groups().delete_membership("mem-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_number_group_membership", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().delete_membership("mem-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_queue", 200, json::object());
  (void)client.queues().del("q-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().del("q-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_recording", 200, json::object());
  (void)client.recordings().delete_("rec-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.recordings().delete_("rec-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_delete_verified_caller_id) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.delete_verified_caller_id", 200, json::object());
  (void)client.verified_callers().del("vc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.delete_verified_caller_id", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().del("vc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.delete_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_get_address) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.get_address", 200, json::object());
  (void)client.addresses().get("ad-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.get_address", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.addresses().get("ad-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_get_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.get_queue", 200, json::object());
  (void)client.queues().get("q-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.get_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().get("q-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_get_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.get_recording", 200, json::object());
  (void)client.recordings().get("rec-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.get_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.recordings().get("rec-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.get_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_addresses) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_addresses", 200, json::object());
  (void)client.addresses().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_addresses", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.addresses().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_brands) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_brands", 200, json::object());
  (void)client.registry().brands.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_brands"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_brands", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().brands.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_brands"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_campaigns) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_campaigns", 200, json::object());
  (void)client.registry().brands.list_campaigns("br-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_campaigns"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_campaigns", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().brands.list_campaigns("br-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_campaigns"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_number_assignments) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_number_assignments", 200, json::object());
  (void)client.registry().campaigns.list_numbers("cm-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_assignments"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_number_assignments", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().campaigns.list_numbers("cm-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_assignments"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_number_group_memberships) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_number_group_memberships", 200, json::object());
  (void)client.number_groups().list_memberships("ng-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_group_memberships"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_number_group_memberships", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().list_memberships("ng-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_group_memberships"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_number_groups) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_number_groups", 200, json::object());
  (void)client.number_groups().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_groups"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_number_groups", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_number_groups"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_orders) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_orders", 200, json::object());
  (void)client.registry().campaigns.list_orders("cm-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_orders"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_orders", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().campaigns.list_orders("cm-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_orders"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_phone_numbers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_phone_numbers", 200, json::object());
  (void)client.phone_numbers().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_phone_numbers", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_queue_members) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_queue_members", 200, json::object());
  (void)client.queues().list_members("q-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_queue_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_queue_members", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().list_members("q-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_queue_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_queues) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_queues", 200, json::object());
  (void)client.queues().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_queues"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_queues", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_queues"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_recordings) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_recordings", 200, json::object());
  (void)client.recordings().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_recordings", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.recordings().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_short_codes) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_short_codes", 200, json::object());
  (void)client.short_codes().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_short_codes"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_short_codes", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.short_codes().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_short_codes"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_list_verified_caller_ids) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.list_verified_caller_ids", 200, json::object());
  (void)client.verified_callers().list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_verified_caller_ids"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.list_verified_caller_ids", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.list_verified_caller_ids"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_lookup_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.lookup_phone_number", 200, json::object());
  (void)client.lookup().lookup("+15550001111");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.lookup_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.lookup_phone_number", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.lookup().lookup("+15550001111");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.lookup_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_purchase_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.purchase_phone_number", 200, json::object());
  (void)client.phone_numbers().create({{"number", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.purchase_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.purchase_phone_number", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().create({{"number", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.purchase_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_redial_verification_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.redial_verification_call", 200, json::object());
  (void)client.verified_callers().redial_verification("vc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.redial_verification_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.redial_verification_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().redial_verification("vc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.redial_verification_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_release_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.release_phone_number", 200, json::object());
  (void)client.phone_numbers().del("pn-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.release_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.release_phone_number", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().del("pn-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.release_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_request_mfa_call) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.request_mfa_call", 200, json::object());
  (void)client.mfa().call({{"to", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.request_mfa_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.request_mfa_call", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.mfa().call({{"to", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.request_mfa_call"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_request_mfa_sms) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.request_mfa_sms", 200, json::object());
  (void)client.mfa().sms({{"to", "+15550001111"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.request_mfa_sms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.request_mfa_sms", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.mfa().sms({{"to", "+15550001111"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.request_mfa_sms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_brand) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_brand", 200, json::object());
  (void)client.registry().brands.get("br-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_brand"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_brand", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().brands.get("br-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_brand"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_campaign) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_campaign", 200, json::object());
  (void)client.registry().campaigns.get("cm-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_campaign", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().campaigns.get("cm-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_next_queue_member) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_next_queue_member", 200, json::object());
  (void)client.queues().get_next_member("q-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_next_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_next_queue_member", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().get_next_member("q-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_next_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_number_group) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_number_group", 200, json::object());
  (void)client.number_groups().get("ng-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_number_group", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().get("ng-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_number_group_membership) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_number_group_membership", 200, json::object());
  (void)client.number_groups().get_membership("mem-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_number_group_membership", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().get_membership("mem-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_number_group_membership"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_order) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_order", 200, json::object());
  (void)client.registry().orders.get("or-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_order"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_order", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().orders.get("or-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_order"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_phone_number", 200, json::object());
  (void)client.phone_numbers().get("pn-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_phone_number", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().get("pn-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_queue_member) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_queue_member", 200, json::object());
  (void)client.queues().get_member("q-1", "m-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_queue_member", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().get_member("q-1", "m-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_queue_member"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_short_code) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_short_code", 200, json::object());
  (void)client.short_codes().get("sc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_short_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_short_code", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.short_codes().get("sc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_short_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_sip_profile) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_sip_profile", 200, json::object());
  (void)client.sip_profile().get();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_sip_profile"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_sip_profile", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.sip_profile().get();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_sip_profile"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_retrieve_verified_caller_id) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.retrieve_verified_caller_id", 200, json::object());
  (void)client.verified_callers().get("vc-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.retrieve_verified_caller_id", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().get("vc-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.retrieve_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_search_available_phone_numbers) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.search_available_phone_numbers", 200, json::object());
  (void)client.phone_numbers().search({{"areacode", "555"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.search_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.search_available_phone_numbers", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().search({{"areacode", "555"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.search_available_phone_numbers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_campaign) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_campaign", 200, json::object());
  (void)client.registry().campaigns.update("cm-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_campaign", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.registry().campaigns.update("cm-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_campaign"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_number_group) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_number_group", 200, json::object());
  (void)client.number_groups().update("ng-1", {{"name", "ng2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_number_group", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.number_groups().update("ng-1", {{"name", "ng2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_number_group"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_phone_number) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_phone_number", 200, json::object());
  (void)client.phone_numbers().update("pn-1", {{"name", "p2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_phone_number", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.phone_numbers().update("pn-1", {{"name", "p2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_phone_number"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_queue) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_queue", 200, json::object());
  (void)client.queues().update("q-1", {{"name", "q2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_queue", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.queues().update("q-1", {{"name", "q2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_queue"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_short_code) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_short_code", 200, json::object());
  (void)client.short_codes().update("sc-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_short_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_short_code", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.short_codes().update("sc-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_short_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_sip_profile) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_sip_profile", 200, json::object());
  (void)client.sip_profile().update({{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_sip_profile"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_sip_profile", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.sip_profile().update({{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_sip_profile"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_update_verified_caller_id) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.update_verified_caller_id", 200, json::object());
  (void)client.verified_callers().update("vc-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.update_verified_caller_id", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().update("vc-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.update_verified_caller_id"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_validate_verification_code) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.validate_verification_code", 200, json::object());
  (void)client.verified_callers().submit_verification("vc-1", {{"code", "123456"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.validate_verification_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.validate_verification_code", 404,
                         json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.verified_callers().submit_verification("vc-1", {{"code", "123456"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.validate_verification_code"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_relay_rest_verify_mfa_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("relay-rest.verify_mfa_token", 200, json::object());
  (void)client.mfa().verify("mfa-1", {{"token", "123456"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.verify_mfa_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("relay-rest.verify_mfa_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.mfa().verify("mfa-1", {{"token", "123456"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("relay-rest.verify_mfa_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_create_conference_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.create_conference_stream", 200, json::object());
  (void)client.video().conferences.create_stream("conf-1", {{"url", "u"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.create_conference_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.create_stream("conf-1", {{"url", "u"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_conference_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_create_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.create_room", 200, json::object());
  (void)client.video().rooms.create({{"name", "r"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.create_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.create({{"name", "r"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_create_room_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.create_room_stream", 200, json::object());
  (void)client.video().rooms.create_stream("room-1", {{"url", "u"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.create_room_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.create_stream("room-1", {{"url", "u"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_create_room_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.create_room_token", 200, json::object());
  (void)client.video().room_tokens.create({{"room_name", "r"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.create_room_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_tokens.create({{"room_name", "r"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_room_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_create_video_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.create_video_conference", 200, json::object());
  (void)client.video().conferences.create({{"name", "c"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.create_video_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.create({{"name", "c"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.create_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_delete_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.delete_room", 200, json::object());
  (void)client.video().rooms.del("room-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.delete_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.del("room-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_delete_room_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.delete_room_recording", 200, json::object());
  (void)client.video().room_recordings.delete_("rr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_room_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.delete_room_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_recordings.delete_("rr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_room_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_delete_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.delete_stream", 200, json::object());
  (void)client.video().streams.delete_("st-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.delete_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().streams.delete_("st-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_delete_video_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.delete_video_conference", 200, json::object());
  (void)client.video().conferences.del("conf-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.delete_video_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.del("conf-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.delete_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_conference_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_conference_token", 200, json::object());
  (void)client.video().conference_tokens.get("ct-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_conference_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_conference_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conference_tokens.get("ct-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_conference_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_room_by_name) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_room_by_name", 200, json::object());
  (void)client.video().rooms.get("room-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_by_name"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_room_by_name", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.get("room-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_by_name"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_room_recording) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_room_recording", 200, json::object());
  (void)client.video().room_recordings.get("rr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_room_recording", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_recordings.get("rr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_recording"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_room_session) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_room_session", 200, json::object());
  (void)client.video().room_sessions.get("rs-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_session"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_room_session", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_sessions.get("rs-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_room_session"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_stream", 200, json::object());
  (void)client.video().streams.get("st-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().streams.get("st-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_get_video_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.get_video_conference", 200, json::object());
  (void)client.video().conferences.get("conf-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.get_video_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.get("conf-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.get_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_conference_streams) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_conference_streams", 200, json::object());
  (void)client.video().conferences.list_streams("conf-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_conference_streams"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_conference_streams", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.list_streams("conf-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_conference_streams"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_conference_tokens) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_conference_tokens", 200, json::object());
  (void)client.video().conferences.list_conference_tokens("conf-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_conference_tokens"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_conference_tokens", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.list_conference_tokens("conf-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_conference_tokens"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_recording_events) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_recording_events", 200, json::object());
  (void)client.video().room_recordings.list_events("rr-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_recording_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_recording_events", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_recordings.list_events("rr-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_recording_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_recordings) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_recordings", 200, json::object());
  (void)client.video().room_recordings.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_recordings", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_recordings.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_session_events) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_session_events", 200, json::object());
  (void)client.video().room_sessions.list_events("rs-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_session_events", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_sessions.list_events("rs-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_session_members) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_session_members", 200, json::object());
  (void)client.video().room_sessions.list_members("rs-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_session_members", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_sessions.list_members("rs-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_members"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_session_recordings) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_session_recordings", 200, json::object());
  (void)client.video().room_sessions.list_recordings("rs-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_session_recordings", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_sessions.list_recordings("rs-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_session_recordings"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_sessions) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_sessions", 200, json::object());
  (void)client.video().room_sessions.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_sessions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_sessions", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().room_sessions.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_sessions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_room_streams) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_room_streams", 200, json::object());
  (void)client.video().rooms.list_streams("room-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_streams"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_room_streams", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.list_streams("room-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_room_streams"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_rooms) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_rooms", 200, json::object());
  (void)client.video().rooms.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_rooms", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_list_video_conferences) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.list_video_conferences", 200, json::object());
  (void)client.video().conferences.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_video_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.list_video_conferences", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.list_video_conferences"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_reset_conference_token) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.reset_conference_token", 200, json::object());
  (void)client.video().conference_tokens.reset("ct-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.reset_conference_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.reset_conference_token", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conference_tokens.reset("ct-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.reset_conference_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_update_room) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.update_room", 200, json::object());
  (void)client.video().rooms.update("room-1", {{"name", "r2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.update_room", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().rooms.update("room-1", {{"name", "r2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_update_stream) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.update_stream", 200, json::object());
  (void)client.video().streams.update("st-1", {{"x", "y"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.update_stream", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().streams.update("st-1", {{"x", "y"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_stream"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_video_update_video_conference) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("video.update_video_conference", 200, json::object());
  (void)client.video().conferences.update("conf-1", {{"name", "c2"}});
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("video.update_video_conference", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.video().conferences.update("conf-1", {{"name", "c2"}});
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("video.update_video_conference"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_voice_get_voice_log) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("voice.get_voice_log", 200, json::object());
  (void)client.logs().voice.get("vl-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.get_voice_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("voice.get_voice_log", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().voice.get("vl-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.get_voice_log"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_voice_list_voice_log_events) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("voice.list_voice_log_events", 200, json::object());
  (void)client.logs().voice.list_events("vl-1");
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.list_voice_log_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("voice.list_voice_log_events", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().voice.list_events("vl-1");
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.list_voice_log_events"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}

TEST(rest_mock_cov_voice_list_voice_logs) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("voice.list_voice_logs", 200, json::object());
  (void)client.logs().voice.list();
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.list_voice_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  mocktest::scenario_set("voice.list_voice_logs", 404, json{{"error", "not found"}});
  bool threw = false;
  int status = 0;
  try {
    (void)client.logs().voice.list();
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 404);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("voice.list_voice_logs"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 404);
  }
  return true;
}
