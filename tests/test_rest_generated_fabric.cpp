// Copyright (c) 2025 SignalWire
// SPDX-License-Identifier: MIT
//
// AUTO-GENERATED by scripts/generate_rest_tests.py; DO NOT EDIT.
// Regenerate with:  python3 scripts/generate_rest_tests.py
//
// Full-mock REST wire-tests for the 'fabric' namespace (item E). One SUCCESS +
// one ERROR test per implemented canonical route. The call literal is captured
// verbatim from the real generated client by tools/route_registry.cpp; the
// asserted matched_route is the OpenAPI operationId resolved by the mock's own
// SpecLoader — an oracle INDEPENDENT of the resource generator. Included by
// tests/test_main.cpp.
#include "mocktest.hpp"

namespace {
using namespace signalwire::rest;
using nlohmann::json;
}  // namespace

TEST(rest_mock_gen_fabric_ai_agents_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_ai_agent", 200, json::object());
  (void)(client.fabric().ai_agents.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_ai_agent", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_call_flow", 200, json::object());
  (void)(client.fabric().call_flows.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_call_flow", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_conference_room", 200, json::object());
  (void)(client.fabric().conference_rooms.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_conference_room", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_application", 200, json::object());
  (void)(client.fabric().cxml_applications.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_applications.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_script", 200, json::object());
  (void)(client.fabric().cxml_scripts.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_webhook", 200, json::object());
  (void)(client.fabric().cxml_webhooks.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_cxml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_freeswitch_connector", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_freeswitch_connector", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_relay_application", 200, json::object());
  (void)(client.fabric().relay_applications.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_relay_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_endpoint", 200, json::object());
  (void)(client.fabric().sip_endpoints.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_gateway", 200, json::object());
  (void)(client.fabric().sip_gateways.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_sip_gateway", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber", 200, json::object());
  (void)(client.fabric().subscribers.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_deletesipendpoint_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber_sip_endpoint", 200, json::object());
  (void)(client.fabric().subscribers.delete_sip_endpoint("X", "X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_deletesipendpoint_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_subscriber_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.delete_sip_endpoint("X", "X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_script", 200, json::object());
  (void)(client.fabric().swml_scripts.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_webhook", 200, json::object());
  (void)(client.fabric().swml_webhooks.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_swml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_delete_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_resource", 200, json::object());
  (void)(client.fabric().resources.delete_("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("DELETE"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_delete_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.delete_resource", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.delete_("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.delete_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_addresses_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_fabric_addresses", 200, json::object());
  (void)(client.fabric().addresses.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_fabric_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_addresses_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_fabric_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().addresses.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_fabric_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_addresses_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_fabric_address", 200, json::object());
  (void)(client.fabric().addresses.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_fabric_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_addresses_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_fabric_address", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().addresses.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_fabric_address"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resources", 200, json::object());
  (void)(client.fabric().resources.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resources", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resources"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agents", 200, json::object());
  (void)(client.fabric().ai_agents.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agents", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agents"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_ai_agent", 200, json::object());
  (void)(client.fabric().ai_agents.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_ai_agent", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agent_addresses", 200, json::object());
  (void)(client.fabric().ai_agents.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agent_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_ai_agent_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_ai_agent_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_addresses", 200, json::object());
  (void)(client.fabric().call_flows.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_listversions_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_versions", 200, json::object());
  (void)(client.fabric().call_flows.list_versions("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_versions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_listversions_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flow_versions", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.list_versions("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flow_versions"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flows", 200, json::object());
  (void)(client.fabric().call_flows.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flows"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_call_flows", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_call_flows"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_call_flow", 200, json::object());
  (void)(client.fabric().call_flows.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_call_flow", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_room_addresses", 200, json::object());
  (void)(client.fabric().conference_rooms.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_room_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_room_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_room_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_rooms", 200, json::object());
  (void)(client.fabric().conference_rooms.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_conference_rooms", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_conference_rooms"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_conference_room", 200, json::object());
  (void)(client.fabric().conference_rooms.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_conference_room", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_applications", 200, json::object());
  (void)(client.fabric().cxml_applications.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_applications", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_applications.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_application", 200, json::object());
  (void)(client.fabric().cxml_applications.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_applications.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_application_addresses", 200, json::object());
  (void)(client.fabric().cxml_applications.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_application_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_applications.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_scripts", 200, json::object());
  (void)(client.fabric().cxml_scripts.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_scripts", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_script", 200, json::object());
  (void)(client.fabric().cxml_scripts.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_script_addresses", 200, json::object());
  (void)(client.fabric().cxml_scripts.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_script_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhooks", 200, json::object());
  (void)(client.fabric().cxml_webhooks.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhooks", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_webhook", 200, json::object());
  (void)(client.fabric().cxml_webhooks.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_cxml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhook_addresses", 200, json::object());
  (void)(client.fabric().cxml_webhooks.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_cxml_webhook_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_cxml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connectors", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connectors"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connectors", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connectors"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_freeswitch_connector", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_freeswitch_connector", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connector_addresses", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connector_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_freeswitch_connector_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_freeswitch_connector_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_applications", 200, json::object());
  (void)(client.fabric().relay_applications.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_applications", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_applications"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_relay_application", 200, json::object());
  (void)(client.fabric().relay_applications.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_relay_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_application_addresses", 200, json::object());
  (void)(client.fabric().relay_applications.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_relay_application_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_relay_application_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoints", 200, json::object());
  (void)(client.fabric().sip_endpoints.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoints", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_endpoint", 200, json::object());
  (void)(client.fabric().sip_endpoints.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoint_addresses", 200, json::object());
  (void)(client.fabric().sip_endpoints.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoint_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_endpoint_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_endpoint_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_gateways", 200, json::object());
  (void)(client.fabric().sip_gateways.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateways"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_gateways", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateways"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_gateway", 200, json::object());
  (void)(client.fabric().sip_gateways.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_sip_gateway", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_gateway_addresses", 200, json::object());
  (void)(client.fabric().sip_gateways.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateway_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_sip_gateway_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_sip_gateway_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscribers", 200, json::object());
  (void)(client.fabric().subscribers.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscribers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscribers", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscribers"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber", 200, json::object());
  (void)(client.fabric().subscribers.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_addresses", 200, json::object());
  (void)(client.fabric().subscribers.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_listsipendpoints_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_sip_endpoints", 200, json::object());
  (void)(client.fabric().subscribers.list_sip_endpoints("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_listsipendpoints_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_subscriber_sip_endpoints", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.list_sip_endpoints("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_subscriber_sip_endpoints"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_getsipendpoint_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber_sip_endpoint", 200, json::object());
  (void)(client.fabric().subscribers.get_sip_endpoint("X", "X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_getsipendpoint_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_subscriber_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.get_sip_endpoint("X", "X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_scripts", 200, json::object());
  (void)(client.fabric().swml_scripts.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_scripts", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_scripts"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_script", 200, json::object());
  (void)(client.fabric().swml_scripts.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_script_addresses", 200, json::object());
  (void)(client.fabric().swml_scripts.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_script_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_script_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_list_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhooks", 200, json::object());
  (void)(client.fabric().swml_webhooks.list(std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_list_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhooks", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.list(std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhooks"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_webhook", 200, json::object());
  (void)(client.fabric().swml_webhooks.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_swml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhook_addresses", 200, json::object());
  (void)(client.fabric().swml_webhooks.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_swml_webhook_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_swml_webhook_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_get_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_resource", 200, json::object());
  (void)(client.fabric().resources.get("X"));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_get_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.get_resource", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.get("X"));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.get_resource"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_list_addresses_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resource_addresses", 200, json::object());
  (void)(client.fabric().resources.list_addresses("X", std::map<std::string, std::string>{}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("GET"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resource_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_list_addresses_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.list_resource_addresses", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.list_addresses("X", std::map<std::string, std::string>{}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.list_resource_addresses"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_ai_agent", 200, json::object());
  (void)(client.fabric().ai_agents.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PATCH"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_ai_agent", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_webhook", 200, json::object());
  (void)(client.fabric().cxml_webhooks.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PATCH"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_gateway", 200, json::object());
  (void)(client.fabric().sip_gateways.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PATCH"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_gateway", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_updatesipendpoint_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber_sip_endpoint", 200, json::object());
  (void)(client.fabric().subscribers.update_sip_endpoint("X", "X", {}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PATCH"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_updatesipendpoint_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.update_sip_endpoint("X", "X", {}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_webhook", 200, json::object());
  (void)(client.fabric().swml_webhooks.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PATCH"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createembedtoken_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_embeds_token", 200, json::object());
  (void)(client.fabric().tokens.create_embed_token({.token = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_embeds_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createembedtoken_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_embeds_token", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().tokens.create_embed_token({.token = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_embeds_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createguesttoken_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_guest_token", 200, json::object());
  (void)(client.fabric().tokens.create_guest_token({.allowed_addresses = json::array()}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_guest_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createguesttoken_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_guest_token", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().tokens.create_guest_token({.allowed_addresses = json::array()}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_guest_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_ai_agent", 200, json::object());
  (void)(client.fabric().ai_agents.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_ai_agents_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_ai_agent", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().ai_agents.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_ai_agent"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_deployversion_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.deploy_call_flow_version", 200, json::object());
  (void)(client.fabric().call_flows.deploy_version("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.deploy_call_flow_version"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_deployversion_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.deploy_call_flow_version", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.deploy_version("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.deploy_call_flow_version"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_call_flow", 200, json::object());
  (void)(client.fabric().call_flows.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_call_flow", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_conference_room", 200, json::object());
  (void)(client.fabric().conference_rooms.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_conference_room", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_script", 200, json::object());
  (void)(client.fabric().cxml_scripts.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_webhook", 200, json::object());
  (void)(client.fabric().cxml_webhooks.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_webhooks_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_cxml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_webhooks.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_cxml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_freeswitch_connector", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_freeswitch_connector", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_relay_application", 200, json::object());
  (void)(client.fabric().relay_applications.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_relay_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_endpoint", 200, json::object());
  (void)(client.fabric().sip_endpoints.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_gateway", 200, json::object());
  (void)(client.fabric().sip_gateways.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_gateways_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_sip_gateway", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_gateways.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_sip_gateway"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber", 200, json::object());
  (void)(client.fabric().subscribers.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_createsipendpoint_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_sip_endpoint", 200, json::object());
  (void)(client.fabric().subscribers.create_sip_endpoint("X", {.username = "X", .password = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_createsipendpoint_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.create_sip_endpoint("X", {.username = "X", .password = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_script", 200, json::object());
  (void)(client.fabric().swml_scripts.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_create_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_webhook", 200, json::object());
  (void)(client.fabric().swml_webhooks.create(json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_webhooks_create_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_swml_webhook", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_webhooks.create(json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_swml_webhook"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_assigndomainapplication_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_domain_application", 200, json::object());
  (void)(client.fabric().resources.assign_domain_application("X", {.domain_application_id = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_domain_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_assigndomainapplication_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_domain_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.assign_domain_application("X", {.domain_application_id = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_domain_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_assignphoneroute_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_phone_route", 200, json::object());
  (void)(client.fabric().resources.assign_phone_route("X", {.phone_route_id = "X", .handler = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_phone_route"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_resources_assignphoneroute_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.assign_resource_phone_route", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().resources.assign_phone_route("X", {.phone_route_id = "X", .handler = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.assign_resource_phone_route"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createinvitetoken_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_invite_token", 200, json::object());
  (void)(client.fabric().tokens.create_invite_token({.address_id = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_invite_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createinvitetoken_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_invite_token", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().tokens.create_invite_token({.address_id = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_invite_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createsubscribertoken_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_token", 200, json::object());
  (void)(client.fabric().tokens.create_subscriber_token({.reference = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_createsubscribertoken_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.create_subscriber_token", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().tokens.create_subscriber_token({.reference = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.create_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_refreshsubscribertoken_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.refresh_subscriber_token", 200, json::object());
  (void)(client.fabric().tokens.refresh_subscriber_token({.refresh_token = "X"}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("POST"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.refresh_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_tokens_refreshsubscribertoken_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.refresh_subscriber_token", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().tokens.refresh_subscriber_token({.refresh_token = "X"}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.refresh_subscriber_token"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_call_flow", 200, json::object());
  (void)(client.fabric().call_flows.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_call_flows_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_call_flow", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().call_flows.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_call_flow"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_conference_room", 200, json::object());
  (void)(client.fabric().conference_rooms.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_conference_rooms_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_conference_room", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().conference_rooms.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_conference_room"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_application", 200, json::object());
  (void)(client.fabric().cxml_applications.update("X", {}));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_applications_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_applications.update("X", {}));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_script", 200, json::object());
  (void)(client.fabric().cxml_scripts.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_cxml_scripts_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_cxml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().cxml_scripts.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_cxml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_freeswitch_connector", 200, json::object());
  (void)(client.fabric().freeswitch_connectors.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_freeswitch_connectors_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_freeswitch_connector", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().freeswitch_connectors.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_freeswitch_connector"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_relay_application", 200, json::object());
  (void)(client.fabric().relay_applications.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_relay_applications_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_relay_application", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().relay_applications.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_relay_application"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_endpoint", 200, json::object());
  (void)(client.fabric().sip_endpoints.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_sip_endpoints_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_sip_endpoint", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().sip_endpoints.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_sip_endpoint"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber", 200, json::object());
  (void)(client.fabric().subscribers.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_subscribers_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_subscriber", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().subscribers.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_subscriber"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_update_ok) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_script", 200, json::object());
  (void)(client.fabric().swml_scripts.update("X", json::object()));
  {
    auto j = mocktest::journal_last();
    ASSERT_EQ(j.method, std::string("PUT"));
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_TRUE(*j.response_status >= 200 && *j.response_status < 300);
  }
  return true;
}

TEST(rest_mock_gen_fabric_swml_scripts_update_err) {
  auto client = mocktest::make_client();
  mocktest::scenario_set("fabric.update_swml_script", 500, json{{"error", "x"}});
  bool threw = false;
  int status = 0;
  try {
    (void)(client.fabric().swml_scripts.update("X", json::object()));
  } catch (const SignalWireRestError& e) {
    threw = true;
    status = e.status();
  }
  ASSERT_TRUE(threw);
  ASSERT_EQ(status, 500);
  {
    auto j = mocktest::journal_last();
    ASSERT_TRUE(j.matched_route.has_value());
    ASSERT_EQ(*j.matched_route, std::string("fabric.update_swml_script"));
    ASSERT_TRUE(j.response_status.has_value());
    ASSERT_EQ(*j.response_status, 500);
  }
  return true;
}
